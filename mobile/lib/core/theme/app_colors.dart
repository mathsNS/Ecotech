import 'package:flutter/material.dart';

/// Paleta de cores da EcoTech, extraida das variaveis de
/// ecotech/infrastructure/static/css/style.css. Mesma identidade visual da web.
class AppColors {
  AppColors._();

  static const primary = Color(0xFF2D7559);
  static const primaryLight = Color(0xFF52A382);
  static const primaryDark = Color(0xFF1F5242);
  static const secondary = Color(0xFFE8F5F0);
  static const accent = Color(0xFF3B9D74);

  static const textDark = Color(0xFF1A1A1A);
  static const textLight = Color(0xFF6B7280);

  static const background = Color(0xFFFFFFFF);
  static const backgroundAlt = Color(0xFFF9FAFB);
  static const border = Color(0xFFE5E7EB);

  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
}
